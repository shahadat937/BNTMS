import { TraineeListForMIST } from './trainee-list-for-mist';

describe('TraineeListForMIST', () => {
  it('should create an instance', () => {
    expect(new TraineeListForMIST()).toBeTruthy();
  });
});
